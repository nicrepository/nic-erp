info:
  name: Criar usuário
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/auth/register
  body:
    type: json
    data: |-
      {
        "name": "Caio Almeida",
        "email": "caio.almeida@nic-labs.com.br",
        "password": "caio123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
